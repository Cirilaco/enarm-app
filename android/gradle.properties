# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE are ignored when building from the
# command line, check Build, Execution, Deployment → Gradle / Android Gradle.
# However, you can specify settings here to override those.

# If you've defined a local.properties file location, use it to load the
# properties. Otherwise if you specify individual properties, keep the leading
# underscore, to indicate these are private, not part of the "api".

# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE are ignored when building from the
# command line, check Build, Execution, Deployment → Gradle / Android Gradle.
# However, you can specify settings here to override those.

# If you've defined a local.properties file location, use it to load the
# properties. Otherwise if you specify individual properties, keep the leading
# underscore, to indicate these are private, not part of the "api".

android.useAndroidX=true
android.enableJetifier=true

FLIPPER_VERSION=0.182.0

org.gradle.jvmargs=-Xmx4096m
